# 깊이 있게 공부하고 오래 기억하는 법

> 이 문서는 `cs/` 아래 문서들을 "한 번 읽고 휘발"되지 않게, 그리고 추상적으로 아는 것에서
> 멈추지 않게 만들기 위한 **학습 방법론**입니다. `INTERVIEW_STUDY_PLAN.md`가 "무엇을, 언제"
> 공부할지를 정한다면, 이 문서는 "어떻게" 공부해야 남는지를 다룹니다.

---

## 1. 왜 지금 방식은 휘발되는가

- **수동 입력만 반복한다**: 읽기 → 밑줄 → 다시 읽기는 뇌에 "저장" 신호를 주지 않는다.
  익숙해진 느낌(fluency)과 실제로 아는 것(retrieval 가능 여부)은 다르다.
- **인출(retrieval) 연습이 없다**: 정보를 머리 밖에서 다시 넣었다 뺐다 하지 않으면
  장기기억으로 넘어가지 않는다. "안다"는 느낌은 "설명할 수 있다"와 다르다.
- **추상적 텍스트만 소비하고 구체적 경험이 없다**: 개념이 코드/장애/숫자와 연결되지
  않으면 뇌 입장에서 "쓸모없는 정보"로 분류되어 우선순위가 낮아진다.
- **간격 없이 몰아서 본다**: 벼락치기는 시험 직후엔 기억나지만 1~2주 뒤엔 사라진다
  (망각 곡선). 간격을 두고 다시 인출해야 장기기억으로 고정된다.

→ 해결의 핵심은 딱 두 가지: **① 능동적으로 인출하는 연습을 늘리고, ② 간격을 두고
반복한다.**

---

## 2. 뇌과학/학습과학 기반 5원칙

| 원칙 | 한 줄 설명 | 이 레포에 적용 |
|---|---|---|
| **능동적 인출 (Active Recall)** | 다시 읽지 말고, 안 보고 떠올려라 | 문서를 덮고 백지에 개념을 재구성 |
| **간격 반복 (Spaced Repetition)** | 잊어버릴 타이밍에 다시 꺼내야 강해진다 | 1일→3일→7일→14일→30일 재인출 스케줄 |
| **정교화 질문 (Elaboration)** | "왜?", "그래서 뭐가 달라지는데?"를 스스로 반복 | 각 개념마다 "왜 이렇게 설계됐을까"를 3번 자문 |
| **생성 효과 (Generation Effect)** | 답을 읽는 것보다 직접 만들어내는 게 오래 남는다 | 코드를 보기 전에 내가 먼저 구현/예측해보기 |
| **인터리빙 (Interleaving)** | 한 주제만 몰아서 하지 말고 섞어서 복습 | 스레드+GC+JPA를 하루에 나눠서 교차 복습 |

---

## 3. 문서 1개당 학습 프로토콜 (표준 루틴)

`cs/` 문서 하나를 공부할 때는 아래 순서를 그대로 따른다. 전체 40~60분 예상.

### Step 1. 스캔 리딩 (5분)
- 목차/소제목만 먼저 훑는다. 전체 구조를 먼저 머리에 넣는다.
- 이 단계에서 세부 내용을 이해하려 하지 않는다. "이 문서가 뭘 다루는지"만 파악.

### Step 2. 코드로 직접 검증 (15~30분) ★ 가장 중요
- 문서에 있는 개념을 **읽기 전에 먼저 예측**해본다. ("volatile 없으면 어떻게 될까?")
- `src/main/java/com/emoney/...` 아래 해당 주제 전용 practice 패키지를 만들고
  직접 코드를 짜서 돌려본다. 이미 있는 `multithread/Study.java` 같은 예제가 좋은 시작점.
- **일부러 망가뜨린다**: synchronized를 빼본다, 트랜잭션 전파를 바꿔본다,
  인덱스를 지워본다 → 실제로 문제가 재현되는지 눈으로 확인한다.
- 예상과 실제 결과가 다르면 그 차이가 진짜 배움의 지점이다. 왜 달랐는지 기록.

### Step 3. 문서 정독 + 정교화 질문 (10분)
- 이제 문서를 제대로 읽는다. 읽으면서 각 개념에 스스로 질문을 던진다.
  - "왜 이 방식을 선택했을까? 다른 대안은 없었나?"
  - "이게 없으면 실무에서 어떤 문제가 생기는가?"
  - "이전에 알던 개념과 뭐가 비슷하고 뭐가 다른가?" (예: CAS vs synchronized)

### Step 4. 백지 복원 — Feynman 테크닉 (10분)
- 문서와 코드를 덮는다.
- 빈 종이(or 메모장)에 **주니어 개발자에게 설명하듯** 개념을 처음부터 써본다.
- 막히는 지점 = 실제로 이해하지 못한 지점. 그 부분만 문서를 다시 열어 확인.
- 다시 안 보고 이어서 써본다. 끝까지 막힘없이 설명될 때까지 반복 (보통 2~3회).

### Step 5. 셀프 모의 질문 (5분)
- 문서 하단의 "면접 후속 질문"을 가리고 소리 내어 답한다 (글이 아니라 말로 — 실제
  면접은 말로 하기 때문에 다른 근육을 쓴다).
- 막힌 질문은 오답노트(Step 6)에 기록.

### Step 6. 오답노트 기록 (2분)
- 아래 `STUDY_LOG.md` 형식으로 "막혔던 질문 + 정답 요약 + 재복습 날짜"를 남긴다.
- 이 로그가 다음 간격 반복의 재료가 된다.

---

## 4. 간격 반복 스케줄 — Leitner 박스 간단판

새 항목을 익혔으면 정해진 간격으로 "안 보고 인출"만 다시 시도한다. 자세히 다시 읽는 게
아니라 **떠올려보고, 막히면 그때 확인**하는 것이 포인트.

| 복습 회차 | 시점 | 방법 |
|---|---|---|
| 1차 | 학습 당일 | Step 4~5 (위 프로토콜 자체가 1차 복습) |
| 2차 | +1일 | 백지에 3줄 요약 떠올리기 |
| 3차 | +3일 | 면접 질문 소리 내어 답하기 |
| 4차 | +7일 | 관련 코드 다시 짜보기 (예: DCL 패턴 재구현) |
| 5차 | +14일 | 다른 주제와 섞어서 구술 설명 (인터리빙) |
| 6차 | +30일 | 최종 확인 — 여기서 막히면 "약점 리스트"로 이동 |

각 회차에서 **막힘없이 통과하면 다음 회차로**, 막히면 **1차로 리셋**한다 (Leitner 박스
핵심 규칙). 이렇게 하면 잘 아는 건 점점 안 보고, 약한 건 자주 보게 되어 시간이 절약된다.

### 실행 방법 (택 1)
- **가벼운 방법**: 아래 `STUDY_LOG.md` 템플릿을 루트에 만들어 날짜와 체크박스로 관리.
- **자동화 방법**: 원하면 이 후속 작업으로 "오답노트 기반 자동 복습 알림 스크립트"를
  만들어줄 수 있음 (필요하면 요청).

`STUDY_LOG.md` 예시 포맷:

```markdown
## cs/java/java-concurrency-threading.md

- 학습일: 2026-07-07
- 막혔던 질문: ThreadLocal 메모리 누수가 WAS에서 왜 발생하는가?
- 내 답 요약: 스레드풀 재사용 시 ThreadLocal.remove() 안 하면 이전 요청 값이 남음
- 재복습: [x] +1일(07-08)  [ ] +3일(07-10)  [ ] +7일(07-14)  [ ] +14일  [ ] +30일
```

---

## 5. 동기부여가 안 될 때 — 구조적으로 해결하기

의지력에 기대지 말고 아래처럼 **환경/구조**로 해결한다.

1. **결과물을 남긴다**: 문서를 읽기만 하지 말고 매번 코드 커밋을 하나씩 만든다.
   깃 로그에 쌓이는 커밋 = 눈에 보이는 진척. (이 레포는 이미 날짜별 커밋 습관이 있음)
2. **작게 쪼갠다**: "JPA 챕터 끝내기"가 아니라 "N+1 해결법 하나를 코드로 재현하기"처럼
   20~30분 안에 끝나는 단위로 쪼갠다. 시작 저항을 낮추는 게 핵심.
3. **말로 설명하는 걸 습관화**: 소리 내어 설명하면 뇌가 "청중이 있다"고 착각해서
   더 명료하게 정리하려는 압박이 생긴다 (Protégé Effect). 혼자 중얼거려도 효과 있음.
4. **막히는 걸 실패로 보지 않는다**: 백지 복원에서 막히는 것 자체가 학습이 일어나는
   순간이다. 막히지 않으면 오히려 너무 쉬운 걸 반복하고 있다는 신호.
5. **완벽주의를 버린다**: 한 문서를 100% 이해할 때까지 못 넘어가지 않는다. 60~70%
   이해했으면 다음으로 넘어가고, 간격 반복 사이클에서 자연스럽게 채워진다.

---

## 6. 하지 말아야 할 것 (안티패턴)

- ❌ 형광펜/밑줄만 긋고 넘어가기 — fluency 착각을 만들 뿐 인출 능력은 안 늘어난다.
- ❌ 노트를 "예쁘게 정리"하는 데 시간 쓰기 — 정리 자체는 학습이 아니라 편집 작업이다.
- ❌ 문서를 처음부터 끝까지 다시 읽기로 복습하기 — 대신 "안 보고 먼저 떠올리기"를 먼저.
- ❌ 한 주제를 하루에 몰아서 여러 시간 — 인터리빙 없이는 단기기억에서 안 넘어간다.
- ❌ 코드 없이 개념만 — 이 레포처럼 실습 코드가 있는 환경에서 코드를 안 돌려보는 건
  가장 큰 손실.

---

## 7. 이 레포에서 바로 적용하는 방법

1. `cs/<topic>/*.md` 문서 하나를 고른다 (`INTERVIEW_STUDY_PLAN.md`의 주차별 순서 추천).
2. 그 문서에 대응하는 practice 코드를 `src/main/java/com/emoney/<topic>/` 아래 새로
   만들거나 기존 예제(`multithread/Study.java` 등)를 확장한다.
3. 위 "Step 1~6" 프로토콜을 그대로 실행한다.
4. 루트에 `STUDY_LOG.md`를 만들어 오답노트 + 재복습 날짜를 기록한다 (원하면 지금
   템플릿 파일을 바로 만들어줄 수 있음).
5. 매주 `INTERVIEW_STUDY_PLAN.md`의 "완료 기준" 체크박스를 이 프로토콜을 통과한
   항목만 체크한다 — 읽었다고 체크하지 말고, 안 보고 설명 가능할 때만 체크.
